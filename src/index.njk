---
layout: base.njk
title: Parth Posts — Tech, AI & Web Development
description: Deep dives into Artificial Intelligence, web development, and the tools shaping our digital world.
---

<!-- ===== HERO ===== -->
<section class="hero">
    <div class="container">
        <p class="hero-eyebrow animate-fade-up">Est. 2026</p>
        <h1 class="animate-fade-up animate-delay-1">
            Writing on <em>AI</em>, Code<br>& Digital Craft
        </h1>
        <p class="hero-desc animate-fade-up animate-delay-2">
            Honest, deep-dive articles on artificial intelligence, web development, and the tools reshaping how we work and build online.
        </p>
        <div class="hero-meta animate-fade-up animate-delay-3">
            <span class="hero-meta-item">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
                {{ collections.post | length }} Articles
            </span>
            <span class="hero-meta-dot"></span>
            <span class="hero-meta-item">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                Updated Regularly
            </span>
            <span class="hero-meta-dot"></span>
            <span class="hero-meta-item">By Parth</span>
        </div>
    </div>
</section>

<!-- ===== TOP BANNER AD ===== -->
<div class="banner-ad-wrap">
    <div class="container">
        <div class="ad-slot horizontal-ad" aria-label="Advertisement">
            <p class="ad-label">Advertisement</p>
            <!-- AdSense horizontal unit: replace with your <ins class="adsbygoogle"> block (728×90 leaderboard) -->
        </div>
    </div>
</div>

<!-- ===== MAIN HOMEPAGE LAYOUT ===== -->
<div class="container">
    <div class="homepage-layout">

        <!-- ===== FEED MAIN ===== -->
        <div class="feed-main">

            {% set posts = collections.post | reverse %}

            {% if posts | length > 0 %}

            <p class="section-label">
                Latest Articles
            </p>

            <div class="post-list">
                {% for post in posts %}
                <a href="{{ post.url | url }}" class="post-item animate-fade-up" style="animation-delay: {{ loop.index0 * 0.05 }}s;">
                    <div>
                        <!-- Pads single digits with a 0 (e.g., 01, 02, 10, 11) -->
                        <div class="post-item-num">{% if loop.index < 10 %}0{% endif %}{{ loop.index }}</div>
                        
                        <h3>{{ post.data.title }}</h3>
                        <p class="card-desc" style="font-size:0.875rem; margin-bottom:8px; color: var(--text-muted); font-style: italic;">
                            {{ post.data.description }}
                        </p>
                        
                        <div class="post-item-date">
                            <time>{{ post.date.toDateString() }}</time>
                        </div>
                    </div>
                    
                    <!-- Thumbnail: Replace this placeholder div with an <img> tag if you add cover images to your markdown frontmatter -->
                    {% if post.data.image %}
                        <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="post-item-thumb">
                    {% else %}
                        <div class="post-item-thumb-placeholder"></div>
                    {% endif %}
                </a>
                {% endfor %}
            </div>

            {% else %}
            <!-- Empty state -->
            <div style="padding: 60px 0; text-align: center; color: var(--text-muted);">
                <p style="font-family: var(--font-display); font-size: 1.4rem; margin-bottom: 8px;">No posts yet.</p>
                <p style="font-size: 0.9rem;">Add <code>.md</code> files to <code>src/posts/</code> to get started.</p>
            </div>
            {% endif %}

        </div>

        <!-- ===== FEED SIDEBAR ===== -->
        <aside class="feed-sidebar" aria-label="Sidebar">

            <!-- Square / Rectangle Ad -->
            <div class="sidebar-widget animate-fade-up" style="animation-delay: 0.1s;">
                <div class="ad-slot sidebar-ad" aria-label="Advertisement">
                    <p class="ad-label">Advertisement</p>
                    <p class="ad-label-sub">300 × 250</p>
                    <!-- AdSense medium rectangle: paste your <ins class="adsbygoogle"> block here -->
                </div>
            </div>

            <!-- About widget -->
            <div class="sidebar-widget about-widget animate-fade-up" style="animation-delay: 0.2s;">
                <p class="widget-title">About</p>
                <!-- If you are using a metadata.json file, you can change 'Parth' to {{ metadata.author.name }} -->
                <p class="about-widget-name">Parth</p>
                <p>
                    Writing about AI tools, web development, and the evolving digital landscape. 
                    All articles are independently written, researched, and coded.
                </p>
            </div>

            <!-- Dynamic Topics / Tags -->
            <div class="sidebar-widget animate-fade-up" style="animation-delay: 0.3s;">
                <p class="widget-title">Topics</p>
                <div class="topic-list">
                    {# Loop through all collections automatically generated by Eleventy #}
                    {% for tag, tagPosts in collections %}
                        {# Filter out default collections like 'all' or 'post' so only actual topics show #}
                        {% if tag != "all" and tag != "post" %}
                            <a href="/tags/{{ tag | slugify }}/" class="topic-tag">{{ tag }}</a>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>

        </aside>

    </div>
</div>